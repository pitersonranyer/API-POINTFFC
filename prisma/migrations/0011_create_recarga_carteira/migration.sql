CREATE TABLE `RECARGA_CARTEIRA` (
  `id` INTEGER UNSIGNED NOT NULL AUTO_INCREMENT,
  `carteiraId` INTEGER UNSIGNED NOT NULL,
  `valor` DECIMAL(12,2) NOT NULL,
  `provedor` VARCHAR(50) NOT NULL,
  `idPagamentoExterno` VARCHAR(255) NULL,
  `externalReference` VARCHAR(255) NOT NULL,
  `status` ENUM('PENDENTE', 'PROCESSANDO', 'APROVADA', 'REJEITADA', 'CANCELADA', 'EXPIRADA', 'REEMBOLSADA') NOT NULL DEFAULT 'PENDENTE',
  `pixCopiaCola` TEXT NULL,
  `qrCode` MEDIUMTEXT NULL,
  `expiracao` DATETIME(3) NULL,
  `criadoEm` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `atualizadoEm` DATETIME(3) NOT NULL,
  `aprovadoEm` DATETIME(3) NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `RECARGA_CARTEIRA_idPagamentoExterno_key` (`idPagamentoExterno`),
  UNIQUE INDEX `RECARGA_CARTEIRA_externalReference_key` (`externalReference`),
  INDEX `RECARGA_CARTEIRA_carteiraId_idx` (`carteiraId`),
  CONSTRAINT `RECARGA_CARTEIRA_carteiraId_fkey` FOREIGN KEY (`carteiraId`) REFERENCES `CARTEIRA` (`id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
